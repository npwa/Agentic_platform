.venv/

# Agent runtime secrets (Langfuse API keys) -- see .env.example
/.env

# Local vector store / graph artifacts produced by ingestion
chroma_db/
__pycache__/
*.pyc
.pytest_cache/

# Agent write-action output (step 4: human-approved reports)
reports/

# Langfuse self-host secrets (step 5) -- see langfuse/.env.example
langfuse/.env
